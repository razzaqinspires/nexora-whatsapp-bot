import { ensureCharacter, characterStats } from '../lib/rpg.js';
import { renderCharacterCanvas } from '../services/canvas.js';
import { sendButtons, button } from '../services/buttons.js';
export default {name:'character',version:'17.0.0',aliases:['char','stats','statplayer'],usage:'character',help:'Character sheet lengkap RPG',async execute({m,prefix}){const u=ensureCharacter(m),s=characterStats(m),image=await renderCharacterCanvas(u,s);return sendButtons(m,{image,caption:`*CHARACTER v17*\nLv.${s.level} ${u.rank}\nHP ${s.maxHp} • MP ${s.maxMp}\nATK ${s.atk} • MATK ${s.matk}\nDEF ${s.def} • MDEF ${s.mdef}\nCrit ${s.crit.toFixed(1)}% • CDMG ${s.critDmg.toFixed(0)}%\nPen ${s.pen} • M.Pen ${s.magicPen}\nASPD ${s.aspd} • HIT ${s.hit} • EVA ${s.eva}\n\nCore: STR ${s.str} AGI ${s.agi} DEX ${s.dex} INT ${s.int} VIT ${s.vit} LUK ${s.luk} WIS ${s.wis} CHA ${s.cha}`,buttons:[button(`${prefix}inventory`,'Inventory'),button(`${prefix}battleproof`,'Battle Proof'),button(`${prefix}menu`,'Menu')]});}};
