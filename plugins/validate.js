import { validateMessageShape } from '../serializer.js';
export default { version:'17.0.0',  name:'validate', aliases:['validatemsg','inspect'], ownerOnly:true, usage:'validate', help:'Validasi struktur message dan identity serializer', async execute({m}) { const v=validateMessageShape(m.raw,m.key,m.chat,m.identity,m.group); return m.reply({text:`*VALIDATION*\nOK: ${v.ok}\nType: ${v.messageType||'-'}\nCandidates: ${v.identityCandidates}\nGroup metadata: ${v.hasGroupMetadata?'YES':'NO'}\nErrors: ${v.errors.join('; ')||'-'}\nWarnings: ${v.warnings.join('; ')||'-'}`}); } };
