import { levelNotifyEnabled, setLevelNotify } from '../lib/gamification.js';
export default { name:'levelnotify', version:'17.0.0', aliases:['levelnotif'], groupOnly:true, groupAdminOnly:true, usage:'levelnotify on|off|status', help:'Atur notifikasi level-up di grup', async execute({m,args}){const a=(args[0]||'status').toLowerCase();if(a==='status')return m.reply({text:`Level-up notification: *${levelNotifyEnabled(m.chat)?'ON':'OFF'}*`});if(!['on','off'].includes(a))return m.reply({text:'Gunakan .levelnotify on|off|status'});await setLevelNotify(m.chat,a==='on');return m.reply({text:`Notifikasi level-up grup: *${a.toUpperCase()}*`});}};
