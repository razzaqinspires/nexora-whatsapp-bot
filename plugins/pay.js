import { transferCoins, economyHistory } from '../lib/economy.js';
export default {name:'pay',version:'17.0.0',aliases:['transfer','coin'],usage:'pay <jid> <coin>',category:'economy',help:'Transfer coin antar user',async execute({m,args}){if(!args[0]||!args[1])return m.reply({text:'.pay <jid> <coin>\n.pay history'});if(args[0]==='history'){const h=economyHistory(m);return m.reply({text:h.length?h.map(x=>`${x.at} • ${x.amount} coin`).join('\n'):'Belum ada transaksi.'});}try{const r=await transferCoins(m,args[0],args[1]);return m.reply({text:`*TRANSFER BERHASIL*\n${r.amount} coin\nTX: ${r.id}`});}catch(e){return m.reply({text:e.message});}}};
