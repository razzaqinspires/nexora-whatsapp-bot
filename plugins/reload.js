export default { version:'17.0.0',  name:'reload', aliases:['reloadplugins'], ownerOnly:true, usage:'reload', help:'Muat ulang semua plugin', async execute({m,reloadPlugins}) { const count=await reloadPlugins(); await m.reply({text:`Plugin berhasil di-reload. ${count} plugin aktif.`}); } };
